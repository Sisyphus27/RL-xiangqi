---
phase: 04.1-bugfix-legal
verified: 2026-03-21T14:15:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
gaps: []
---

# Phase 04.1: Bugfix Legal — Verification Report

**Phase Goal:** Fix is_legal() geometry validation, legal_moves() performance, and undo test expectation
**Verified:** 2026-03-21T14:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                 | Status     | Evidence                                                      |
| --- | --------------------------------------------------------------------- | ---------- | ------------------------------------------------------------- |
| 1   | `is_legal()` returns False for diagonal chariot move encode_move(81, 73) | VERIFIED   | encode_move(rc_to_sq(9,0), rc_to_sq(8,1)) = False            |
| 2   | `is_legal()` returns False for own-piece destination encode_move(81, 86) | VERIFIED   | encode_move(rc_to_sq(9,0), rc_to_sq(9,5)) = False            |
| 3   | `is_legal()` returns False for any move violating piece-type geometry | VERIFIED   | `_is_valid_geometry()` covers all 7 piece types before board copy; horse leg-occupancy check present (line 72: `board[leg_r, leg_c] == 0`) |
| 4   | `legal_moves()` on starting position completes in under 10ms         | VERIFIED   | avg 0.54ms over 100 iterations (target: <10ms); 58x improvement over 29ms baseline |
| 5   | `test_undo_empty_stack_raises` expects IndexError with message "nothing to undo" | VERIFIED   | test_api.py line 201: `match="nothing to undo"`; pytest passes |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                          | Expected                                       | Status     | Details                                                                 |
| --------------------------------- | --------------------------------------------- | ---------- | ----------------------------------------------------------------------- |
| `src/xiangqi/engine/legal.py`     | `def _is_valid_geometry` function defined     | VERIFIED   | Lines 36-115: 80-line function covering all 7 piece types; called at line 328 BEFORE `snap = state.copy()` (line 330) |
| `src/xiangqi/engine/legal.py`     | Own-piece pre-filter in `generate_legal_moves` | VERIFIED   | Lines 366-371: `to_sq = (m >> 9) & 0x7F`, own-piece check with `continue`; avoids board copy for blocked destinations |
| `tests/test_api.py`               | `match="nothing to undo"` in undo test       | VERIFIED   | Line 201: `with pytest.raises(IndexError, match="nothing to undo"):` |

### Key Link Verification

| From                     | To                    | Via                          | Status | Details                                                                              |
| ------------------------ | --------------------- | ---------------------------- | ------ | ----------------------------------------------------------------------------------- |
| `is_legal_move()` (L311) | `_is_valid_geometry()` (L36) | Called at L328, before L330 `snap = state.copy()` | WIRED  | Geometry validated before board copy; chariot diagonal rejected before copy        |
| `generate_legal_moves()` (L342) | `is_legal_move()` (L311) | Own-piece pre-filter at L370 before L372 call | WIRED  | `continue` skips board copy for own-piece destinations; call still happens for enemies/empty |
| `legal.py` (L36,230,231,272,273,319,327,328,346,368) | `types.py` | `sq_to_rc()` for geometry math | WIRED  | `sq_to_rc` imported at L15; used throughout for geometry calculations             |

### Requirements Coverage

No formal requirement IDs for this gap-closure phase (requirement IDs: null). Internal requirements from plan:
- API-01 (is_legal correctness): SATISFIED — geometry validation + king-safety check both present
- API-04 (performance <10ms): SATISFIED — 0.54ms avg, well under 10ms target

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments in `legal.py`. No stub implementations.

### Human Verification Required

None. All assertions are programmatically verifiable.

### Notable Finding: Prompt Automated Check Contains Known Stale Assertion

The prompt's automated check script asserts `is_legal(encode_move(rc_to_sq(9, 0), rc_to_sq(9, 4))) == True`. This move `(9,0)->(9,4)` is geometrically valid for a chariot but blocked in the starting position by the horse at (9,1). The plan's own SUMMARY (Section "Key Decisions", item 1) documents this was already discovered and corrected during plan execution — the correct sanity check uses the vertical slide `(9,0)->(8,0)`. The VERIFIED sanity check above uses the plan-corrected move and returns True as expected.

### Gaps Summary

None. All five must-haves verified, all artifacts present and wired, all tests pass.

---

_Verified: 2026-03-21T14:15:00Z_
_Verifier: Claude (gsd-verifier)_
