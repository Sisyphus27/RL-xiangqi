---
phase: 14-marl-keyword-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper_collect/dblp_config.json
  - paper_collect/dblp_papers_hypergraph_archive.csv
autonomous: false
requirements:
  - LIT-01
  - LIT-02
  - LIT-03

must_haves:
  truths:
    - "Researcher can open dblp_config.json and see MARL-domain broad search keywords (not hypergraph/traffic)"
    - "Researcher can identify the two-stage keyword structure: Stage 1 flat list for broad retrieval, Stage 2 nested Level 3 AND/OR groups for refined extraction"
    - "Researcher can confirm aamas and jaamas are listed among target venues in dblp_config.json"
    - "A test run of dblp_search.py with the new config returns non-zero results"
  artifacts:
    - path: "paper_collect/dblp_config.json"
      provides: "MARL-domain two-stage keyword configuration"
      contains: "aamas"
    - path: "paper_collect/dblp_papers_hypergraph_archive.csv"
      provides: "Archived hypergraph CSV to prevent domain mixing"
      min_lines: 100
  key_links:
    - from: "paper_collect/dblp_search.py"
      to: "paper_collect/dblp_config.json"
      via: "reads search.default_keywords and search.default_venues"
      pattern: "default_keywords"
    - from: "paper_collect/dblp_keywords_extract.py"
      to: "paper_collect/dblp_config.json"
      via: "reads extract.default_keywords"
      pattern: "extract"
---

<objective>
Replace the hypergraph/traffic keyword configuration in dblp_config.json with a two-stage MARL-domain keyword design, add AAMAS/JAAMAS venues, narrow year range to 2024-2026, and verify the pipeline works with a calibration test run.

Purpose: Enable the existing DBLP search pipeline to collect MARL literature relevant to heterogeneous agent predictive collaboration, anchored by the AIM paper (Wu et al., 2025).

Output: Updated dblp_config.json with MARL keywords, archived hypergraph CSV, verified pipeline connectivity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-marl-keyword-design/14-CONTEXT.md
@.planning/phases/14-marl-keyword-design/14-RESEARCH.md
@paper_collect/dblp_config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dblp_config.json content with MARL two-stage keywords</name>
  <files>paper_collect/dblp_config.json</files>
  <read_first>
    - paper_collect/dblp_config.json (current file to modify -- preserve all infrastructure sections)
    - .planning/phases/14-marl-keyword-design/14-CONTEXT.md (user-locked decisions D-01 through D-08)
    - .planning/phases/14-marl-keyword-design/14-RESEARCH.md (Section 2: keyword structure, Section 5: venues, Section 6.3: JSON structure precision)
  </read_first>
  <action>
    Modify paper_collect/dblp_config.json. Preserve the top-level keys: search, api, pagination, termination, extract, abstract_apis. Only change the content values described below. Keep all infrastructure settings (api, pagination, termination, abstract_apis) identical to current values.

    **1. Replace search.default_keywords** (currently 35 hypergraph/traffic terms) with these 26 MARL terms, in this exact order:

    MARL Core (6): "multi-agent reinforcement learning", "multi-agent RL", "MARL", "cooperative multi-agent", "decentralized execution", "CTDE"
    Heterogeneous and Roles (7): "heterogeneous agent", "heterogeneous multi-agent", "agent diversity", "asymmetric agents", "type-aware", "role-based", "diverse agent"
    Modeling and Inference (6): "teammate modeling", "opponent modeling", "active inference", "Theory of Mind", "belief tracking", "agent modeling"
    Value Decomposition and Policy (4): "value decomposition", "credit assignment", "MAPPO", "QMIX"
    Collaboration and Communication (3): "communication-free", "ad-hoc teamwork", "cooperative coordination"

    Implements D-01, D-02, D-03.

    **2. Replace search.default_venues** -- keep all 18 existing venues in current order, append "aamas" and "jaamas" at the end (total 20 venues).

    Implements D-06.

    **3. Change search.start_year** from 2020 to 2024. Keep search.end_year as 2026. Implements D-07.

    **4. Replace extract.default_keywords** (currently 14 Level 2 hypergraph groups) with a Level 3 (3D) array of exactly 6 topic groups. Each outer element is a Level 2 AND/OR query (2D array of string arrays). The outermost array is Level 3 -- extract_keywords_multi_group() runs each Level 2 query independently and merges with deduplication. This means a paper matches if it satisfies ANY of the 6 topic groups.

    Group 1 (MARL Core + Heterogeneity):
    - AND group A: ["multi-agent reinforcement learning", "multi-agent RL", "MARL"]
    - AND group B: ["heterogeneous", "diverse", "asymmetric", "type-aware", "role-based", "different action space"]

    Group 2 (MARL Core + Cooperation):
    - AND group A: ["multi-agent reinforcement learning", "multi-agent RL", "MARL"]
    - AND group B: ["cooperative", "collaborative", "coordination"]

    Group 3 (Modeling/Inference + Cooperation):
    - AND group A: ["teammate modeling", "opponent modeling", "agent modeling", "active inference", "Theory of Mind", "belief"]
    - AND group B: ["cooperative", "collaborative", "coordination", "multi-agent"]

    Group 4 (Heterogeneity + Training):
    - AND group A: ["heterogeneous", "diverse", "asymmetric", "type-aware", "role-based", "different action space"]
    - AND group B: ["decentralized", "CTDE", "value decomposition", "policy gradient", "credit assignment"]

    Group 5 (Collaboration + No Communication):
    - AND group A: ["communication-free", "ad-hoc teamwork", "non-communication"]
    - AND group B: ["multi-agent", "cooperative", "collaboration", "coordination"]

    Group 6 (Application Scenario + MARL):
    - AND group A: ["game", "board game", "StarCraft", "planning", "scheduling", "robot"]
    - AND group B: ["multi-agent reinforcement learning", "multi-agent RL", "MARL", "cooperative"]

    Implements D-04, D-05.

    **5. Do NOT change:** api, pagination, termination, or any extract settings (default_csv, default_match_all, default_title_col, output_prefix, max_base_len, max_filename_length, case_insensitive, strip_accents, return_matches), or abstract_apis. These all remain identical to current values. Implements D-08.
  </action>
  <verify>
    <automated>cd "D:/WorkSpace/RL-xiangqi" && python -c "
import json
c = json.load(open('paper_collect/dblp_config.json'))
k = c['search']['default_keywords']
v = c['search']['default_venues']
e = c['extract']['default_keywords']
assert len(k) == 26, f'Expected 26 keywords, got {len(k)}'
assert 'aamas' in v and 'jaamas' in v, 'Missing AAMAS venues'
assert len(v) == 20, f'Expected 20 venues, got {len(v)}'
assert c['search']['start_year'] == 2024, 'Expected start_year 2024'
assert c['search']['end_year'] == 2026, 'Expected end_year 2026'
assert len(e) == 6, f'Expected 6 extract groups, got {len(e)}'
for i, g in enumerate(e):
    assert isinstance(g, list), f'Group {i} must be list'
    for j, s in enumerate(g):
        assert isinstance(s, list), f'Group {i} subgroup {j} must be list'
        for kw in s:
            assert isinstance(kw, str), f'Group {i} subgroup {j} keyword must be str'
assert 'hypergraph' not in str(k), 'Old hypergraph keywords still present'
assert 'traffic' not in str(k), 'Old traffic keywords still present'
assert c['api']['base_url'] == 'https://dblp.org/search/publ/api', 'API URL changed'
assert c['pagination']['max_workers'] == 2, 'Pagination settings changed'
print('PASS: 26 MARL keywords, 20 venues, start_year=2024, 6 Level-3 extract groups, infrastructure preserved')
"</automated>
  </verify>
  <acceptance_criteria>
    - paper_collect/dblp_config.json contains exactly 26 entries in search.default_keywords
    - paper_collect/dblp_config.json contains "aamas" and "jaamas" in search.default_venues
    - paper_collect/dblp_config.json has search.start_year == 2024
    - paper_collect/dblp_config.json has exactly 6 elements in extract.default_keywords, each being a 2D array
    - paper_collect/dblp_config.json does NOT contain "hypergraph" or "traffic" in search.default_keywords
    - paper_collect/dblp_config.json preserves api.base_url as "https://dblp.org/search/publ/api"
    - paper_collect/dblp_config.json preserves pagination.max_workers as 2
  </acceptance_criteria>
  <done>dblp_config.json contains 26 MARL-domain broad search keywords, 20 venues including aamas and jaamas, start_year=2024, and 6 Level-3 AND/OR extract groups. No hypergraph/traffic keywords remain. All infrastructure settings preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Archive old CSV and run calibration test of dblp_search.py</name>
  <files>paper_collect/dblp_papers_hypergraph_archive.csv</files>
  <read_first>
    - paper_collect/dblp_papers.csv (the existing 5,597-row hypergraph CSV to archive)
    - paper_collect/dblp_search.py (understand the script's CLI interface for running it)
    - paper_collect/dblp_config.json (verify the updated config before running)
  </read_first>
  <what-built>
    Updated dblp_config.json with MARL-domain keywords (Task 1). Now need to archive the existing hypergraph CSV and run a quick calibration test to confirm the pipeline produces results.
  </what-built>
  <action>
    **Step 1: Archive the existing hypergraph CSV.**
    Copy paper_collect/dblp_papers.csv to paper_collect/dblp_papers_hypergraph_archive.csv. This prevents domain mixing when the new MARL search results are generated in Phase 15.

    ```bash
    cp paper_collect/dblp_papers.csv paper_collect/dblp_papers_hypergraph_archive.csv
    ```

    **Step 2: Run a limited calibration test.**
    Run dblp_search.py with the updated config. This is a sanity check to confirm the MARL keywords produce results from DBLP. The full pipeline run happens in Phase 15 -- this test just needs to return non-zero results.

    Before running the full search, consider doing a quick test with reduced scope to save time:
    - Option A: Run the full search (26 keywords x 20 venues = ~520 queries, estimated ~9 min). This produces the actual broad CSV for Phase 15.
    - Option B: Run a subset test by temporarily modifying config to use fewer keywords and venues, then restore full config. This is faster but requires two config edits.

    Recommended: Option A. The ~9 minute runtime is acceptable and produces the actual data file needed for Phase 15.

    Run from the project root:
    ```bash
    cd "D:/WorkSpace/RL-xiangqi" && conda activate xqrl && python paper_collect/dblp_search.py
    ```

    The script reads paper_collect/dblp_config.json automatically. It will generate/update paper_collect/dblp_papers.csv with MARL-domain results.

    **Step 3: Verify results.**
    Check that paper_collect/dblp_papers.csv exists and has non-zero rows (different from the archived hypergraph data):
    ```bash
    wc -l paper_collect/dblp_papers.csv
    ```
    The CSV should have a header row plus at least some data rows. Volume calibration (targeting 5,000-10,000) happens in Phase 15 -- any non-zero result count confirms the pipeline works.
  </action>
  <how-to-verify>
    1. Confirm paper_collect/dblp_papers_hypergraph_archive.csv exists and has ~5,597 rows:
       `wc -l paper_collect/dblp_papers_hypergraph_archive.csv`
    2. Run the DBLP search: `conda activate xqrl && python paper_collect/dblp_search.py`
    3. After search completes, check new CSV: `wc -l paper_collect/dblp_papers.csv`
    4. Expected: new CSV has a header row + data rows (non-zero). The exact count depends on DBLP results but should be positive.
    5. Spot-check: open paper_collect/dblp_papers.csv and confirm the titles contain MARL-related terms (not hypergraph/traffic terms).
  </how-to-verify>
  <acceptance_criteria>
    - paper_collect/dblp_papers_hypergraph_archive.csv exists with ~5,597 rows (original hypergraph data preserved)
    - paper_collect/dblp_papers.csv has been regenerated with MARL-domain results
    - paper_collect/dblp_papers.csv contains at least 1 data row (header + data)
    - Titles in new dblp_papers.csv contain MARL-related terms (not hypergraph/traffic terms)
  </acceptance_criteria>
  <done>Hypergraph CSV archived. dblp_search.py runs successfully with MARL config and produces non-zero results. Pipeline connectivity confirmed.</done>
</task>

</tasks>

<verification>
1. Config validation: `python -c "import json; c=json.load(open('paper_collect/dblp_config.json')); assert len(c['search']['default_keywords'])==26; assert 'aamas' in c['search']['default_venues']; assert c['search']['start_year']==2024; assert len(c['extract']['default_keywords'])==6; print('CONFIG OK')"`
2. Archive exists: `test -f paper_collect/dblp_papers_hypergraph_archive.csv && echo "ARCHIVE OK"`
3. New CSV has results: `python -c "import csv; r=list(csv.reader(open('paper_collect/dblp_papers.csv'))); print(f'{len(r)-1} papers found')"`
</verification>

<success_criteria>
- dblp_config.json has 26 MARL broad search keywords (zero hypergraph/traffic terms)
- dblp_config.json has 6 Level-3 AND/OR extract groups with correct nesting
- dblp_config.json has 20 venues including aamas and jaamas
- dblp_config.json has start_year=2024, end_year=2026
- All infrastructure settings (api, pagination, termination, abstract_apis) unchanged
- dblp_papers_hypergraph_archive.csv preserves original hypergraph data
- dblp_search.py runs successfully and returns non-zero MARL results
</success_criteria>

<output>
After completion, create `.planning/phases/14-marl-keyword-design/14-01-SUMMARY.md`
</output>
