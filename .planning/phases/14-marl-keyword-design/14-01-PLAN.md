---
phase: 14-marl-keyword-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper_collect/dblp_config.json
autonomous: true
requirements:
  - LIT-01
  - LIT-02
  - LIT-03

must_haves:
  truths:
    - "dblp_config.json contains MARL-domain keywords, not hypergraph/traffic keywords"
    - "Stage 1 has ~25 keywords across 5 semantic groups (MARL Core, Heterogeneous, Modeling, Value Decomp, Collaboration)"
    - "Stage 2 has 6 AND/OR groups with Level 2 nesting — inner lists are OR synonyms, outer list is AND"
    - "AAMAS is present in the venues list (covers both AAMAS conf and JAAMAS journal)"
    - "Year range is 2024-2026"
    - "A test DBLP query returns non-zero results, confirming config validity"
  artifacts:
    - path: "paper_collect/dblp_config.json"
      provides: "Complete two-stage MARL keyword configuration"
      contains: "heterogeneous agent"
  key_links:
    - from: "paper_collect/dblp_config.json"
      to: "paper_collect/dblp_search.py"
      via: "search.default_keywords and search.default_venues"
      pattern: "default_keywords.*multi-agent"
    - from: "paper_collect/dblp_config.json"
      to: "paper_collect/dblp_keywords_extract.py"
      via: "extract.default_keywords (Level 2 nested AND/OR)"
      pattern: "default_keywords.*\\[\\["
---

<objective>
Replace hypergraph/traffic keywords in dblp_config.json with MARL-domain keywords following the two-stage design from CONTEXT.md decisions D-01 through D-08, then validate with a calibration test against the DBLP API.

Purpose: Deliver a ready-to-run keyword configuration that produces targeted MARL search results via the existing pipeline, enabling Phase 15 paper collection.

Output: Updated dblp_config.json with Stage 1 broad keywords (~26 terms in 5 groups), Stage 2 refined AND/OR groups (6 groups), updated venues (+aamas), and narrowed year range (2024-2026).
</objective>

<execution_context>
@/Users/zy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-marl-keyword-design/14-CONTEXT.md
@.planning/phases/14-marl-keyword-design/14-RESEARCH.md
@.planning/phases/14-marl-keyword-design/14-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MARL keyword configuration into dblp_config.json</name>
  <files>paper_collect/dblp_config.json</files>
  <read_first>
    - paper_collect/dblp_config.json (current file to modify — preserve structure, replace content)
    - .planning/phases/14-marl-keyword-design/14-CONTEXT.md (locked decisions D-01 through D-08)
  </read_first>
  <action>
    Rewrite `paper_collect/dblp_config.json` with the following content changes. Preserve the JSON structure exactly — only the content values in three sections change, plus `search.start_year`. All other sections (`api`, `pagination`, `termination`, `abstract_apis`) remain unchanged.

    **Section 1 — `search.default_keywords` (Stage 1 broad search):**
    Replace the entire 34-item hypergraph/traffic keyword list with exactly these 26 keywords organized by semantic group (order does not affect search, but group logically for readability):

    MARL Core group:
    - "multi-agent reinforcement learning"
    - "multi-agent RL"
    - "MARL"
    - "cooperative multi-agent"
    - "decentralized execution"
    - "CTDE"

    Heterogeneous & Roles group (expanded — 7 keywords per D-03):
    - "heterogeneous agent"
    - "heterogeneous multi-agent"
    - "agent diversity"
    - "asymmetric agents"
    - "type-aware"
    - "role-based"
    - "diverse agent"

    Modeling & Inference group:
    - "teammate modeling"
    - "opponent modeling"
    - "active inference"
    - "Theory of Mind"
    - "belief tracking"
    - "agent modeling"

    Value Decomposition & Policy group:
    - "value decomposition"
    - "credit assignment"
    - "MAPPO"
    - "QMIX"

    Collaboration & Communication group:
    - "communication-free"
    - "ad-hoc teamwork"
    - "cooperative coordination"

    Total: 26 keywords (close to ~25 target per D-02).

    **Section 2 — `search.default_venues`:**
    Keep all 18 existing venues, then append "aamas" as the 19th venue. The final list is:
    "kdd", "icml", "neurips", "aaai", "ijcai", "iclr", "icde", "sigmod", "vldb", "tkde", "tmc", "www", "cvpr", "acl", "iccv", "pami", "sigir", "tpami", "aamas"

    Note: "aamas" alone covers both AAMAS conference and JAAMAS journal via substring matching in dblp_search.py.

    **Section 3 — `search.start_year`:**
    Change from 2020 to 2024 (per D-07). `search.end_year` stays 2026.

    **Section 4 — `extract.default_keywords` (Stage 2 refined extraction):**
    Replace the entire 15-row hypergraph extraction list with exactly these 6 AND/OR groups. This is a Level 2 nested array: `[[OR-group1], [OR-group2], ..., [OR-group6]]` where inner lists are OR synonyms and the outer list is AND (paper must match at least one term from EVERY group).

    Group 1 — MARL Core:
    ["multi-agent reinforcement learning", "multi-agent RL", "MARL"]

    Group 2 — Cooperation/Coordination:
    ["cooperative", "collaborative", "coordination"]

    Group 3 — Heterogeneity/Diversity (richest synonym coverage per D-05):
    ["heterogeneous", "diverse", "asymmetric", "type-aware", "role-based", "different action space"]

    Group 4 — Modeling/Inference:
    ["teammate modeling", "opponent modeling", "agent modeling", "active inference", "Theory of Mind", "belief"]

    Group 5 — Execution/Training:
    ["decentralized", "CTDE", "value decomposition", "policy gradient", "credit assignment"]

    Group 6 — Application Scenario:
    ["game", "board game", "StarCraft", "planning", "scheduling", "robot"]

    **Sections NOT changed:**
    - `api` — all values unchanged (base_url, timeout, retry_total, retry_backoff, max_attempts, thread_timeout)
    - `pagination` — all values unchanged (max_per_page, max_pages, sleep_time, jitter, max_workers, max_loops)
    - `termination` — all values unchanged
    - `abstract_apis` — all values unchanged (semantic_scholar.enable stays false)
    - `extract.default_csv` — unchanged ("paper_collect/dblp_papers.csv")
    - `extract.default_match_all` — unchanged (true)
    - `extract.default_title_col` — unchanged ("title")
    - `extract.output_prefix` — unchanged ("dblp_papers_matched")
    - `extract.max_base_len` — unchanged (50)
    - `extract.max_filename_length` — unchanged (200)
    - `extract.case_insensitive` — unchanged (true)
    - `extract.strip_accents` — unchanged (false)
    - `extract.return_matches` — unchanged (true)
    - `search.max_results` — unchanged (40000)
    - `search.end_year` — unchanged (2026)
  </action>
  <verify>
    python -c "
import json
c = json.load(open('paper_collect/dblp_config.json'))

# Stage 1: 26 MARL keywords, no hypergraph/traffic remnants
kw = c['search']['default_keywords']
assert len(kw) == 26, f'Expected 26 keywords, got {len(kw)}'
assert 'heterogeneous agent' in kw, 'Missing heterogeneous agent'
assert 'active inference' in kw, 'Missing active inference'
assert 'hypergraph' not in kw, 'Old hypergraph keyword still present'
assert 'traffic prediction' not in kw, 'Old traffic keyword still present'
assert 'taxi' not in kw, 'Old taxi keyword still present'

# Stage 2: 6 AND/OR groups, Level 2 nesting
ekw = c['extract']['default_keywords']
assert len(ekw) == 6, f'Expected 6 extraction groups, got {len(ekw)}'
assert all(isinstance(g, list) for g in ekw), 'Not all groups are lists'
assert 'heterogeneous' in ekw[2], 'Group 3 missing heterogeneous'
assert len(ekw[2]) == 6, f'Group 3 should have 6 synonyms, got {len(ekw[2])}'

# Venues: 19 venues including aamas
venues = c['search']['default_venues']
assert len(venues) == 19, f'Expected 19 venues, got {len(venues)}'
assert 'aamas' in venues, 'Missing aamas venue'

# Year range: 2024-2026
assert c['search']['start_year'] == 2024, f'Expected start_year 2024, got {c[\"search\"][\"start_year\"]}'
assert c['search']['end_year'] == 2026

# API/pagination/termination unchanged
assert c['api']['base_url'] == 'https://dblp.org/search/publ/api'
assert c['pagination']['sleep_time'] == 2.0
assert c['termination']['stop_on_duplicate_page'] == True

print('ALL CHECKS PASSED')
"
  </verify>
  <acceptance_criteria>
    - paper_collect/dblp_config.json contains exactly 26 items in search.default_keywords
    - paper_collect/dblp_config.json search.default_keywords contains "heterogeneous agent", "active inference", "CTDE", "MAPPO", "ad-hoc teamwork"
    - paper_collect/dblp_config.json search.default_keywords does NOT contain "hypergraph", "traffic", "taxi", "OD prediction"
    - paper_collect/dblp_config.json extract.default_keywords has exactly 6 groups (Level 2 nested arrays)
    - paper_collect/dblp_config.json extract.default_keywords Group 3 has 6 synonyms including "heterogeneous", "different action space"
    - paper_collect/dblp_config.json search.default_venues has 19 items including "aamas"
    - paper_collect/dblp_config.json search.start_year equals 2024
    - paper_collect/dblp_config.json api, pagination, termination, abstract_apis sections are identical to original
  </acceptance_criteria>
  <done>dblp_config.json contains 26 MARL-domain Stage 1 keywords across 5 semantic groups, 6 Stage 2 AND/OR extraction groups with Level 2 nesting, 19 venues including aamas, year range 2024-2026, and all infrastructure settings unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Validate config with DBLP API calibration test</name>
  <files></files>
  <read_first>
    - paper_collect/dblp_config.json (verify it was written correctly by reading it)
    - paper_collect/dblp_search.py (understand CLI args for test invocation)
  </read_first>
  <action>
    Run a minimal calibration test against the DBLP API to verify:
    1. The new config is parseable by dblp_search.py
    2. DBLP API connectivity works
    3. At least one keyword-venue pair returns results

    Execute this command:
    ```
    cd /Users/zy/.ssh/RL-xiangqi && conda run -n xqrl python paper_collect/dblp_search.py --keyword "MARL" --venue "neurips" --start-year 2024 --end-year 2026 --max-workers 1 --max-pages 2 -o paper_collect/dblp_test_calibration.csv --log-level INFO
    ```

    This runs a single keyword-venue pair ("MARL neurips") with minimal pagination, which should complete in under 30 seconds and produce a small CSV with results.

    After the command completes:
    - Verify the CSV file exists and is non-empty: `wc -l paper_collect/dblp_test_calibration.csv` should show at least 2 lines (header + 1 result)
    - Read the first few lines to confirm the data looks reasonable (title, year, venue, doi columns present)

    Then clean up the test file: `rm paper_collect/dblp_test_calibration.csv`

    If the API call fails with a network error, that is acceptable — it means the config is structurally valid but DBLP was unreachable. The structural validation in Task 1's verify step already confirms config correctness. Log the error and proceed.
  </action>
  <verify>
    cd /Users/zy/.ssh/RL-xiangqi && python -c "
import json
# Re-verify config is valid JSON and has the expected structure
c = json.load(open('paper_collect/dblp_config.json'))
assert len(c['search']['default_keywords']) == 26
assert len(c['extract']['default_keywords']) == 6
assert 'aamas' in c['search']['default_venues']
print('Config structure valid')
" && echo "STRUCTURAL CHECK PASSED"
  </verify>
  <acceptance_criteria>
    - paper_collect/dblp_config.json parses as valid JSON
    - conda run -n xqrl python paper_collect/dblp_search.py --keyword "MARL" --venue "neurips" --start-year 2024 --end-year 2026 executes without Python import errors or config parse errors
    - Either: (a) a test CSV file with at least 1 result is produced, confirming DBLP connectivity; or (b) a network error occurs, which is acceptable since structural validation already passed
    - Test CSV file is cleaned up after the test
  </acceptance_criteria>
  <done>Config file is confirmed parseable by dblp_search.py and a DBLP API test query either returns results or fails gracefully with a network error (not a config error).</done>
</task>

</tasks>

<verification>
1. Config structural validation (Task 1 verify): `python -c "import json; ..."` — all assertions pass
2. Pipeline integration test (Task 2): `dblp_search.py` reads new config without errors
3. DBLP API connectivity: single keyword-venue query returns results or network error (not config error)
</verification>

<success_criteria>
- dblp_config.json has 26 MARL Stage 1 keywords (zero hypergraph/traffic remnants)
- dblp_config.json has 6 Level 2 nested AND/OR extraction groups
- dblp_config.json has 19 venues including "aamas"
- dblp_config.json has start_year=2024, end_year=2026
- dblp_search.py can parse the config without errors
- A test DBLP query returns results (confirming the keyword design produces hits)
</success_criteria>

<output>
After completion, create `.planning/phases/14-marl-keyword-design/14-01-SUMMARY.md`
</output>
