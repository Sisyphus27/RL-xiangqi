---
phase: 02-move-generation
plan: '02'
type: execute
wave: '2'
depends_on:
  - '01'
files_modified:
  - tests/test_perft.py
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "perft(1) from starting position = 44 nodes (CPW verified)"
    - "perft(2) from starting position = 1,920 nodes (CPW verified, NOT 1,916 from REQUIREMENTS.md)"
    - "perft(3) from starting position = 79,666 nodes (CPW verified, NOT 72,987 from REQUIREMENTS.md)"
    - "perft(4) from starting position = 3,290,240 nodes (CPW verified)"
    - "test_requirements_md_values_fail proves REQUIREMENTS.md values are wrong"
  artifacts:
    - path: "tests/test_perft.py"
      provides: "Perft correctness tests cross-validated against CPW/Fairy-Stockfish reference"
      exports: ["_perft", "CPW_PERFT", "TestPerftStartingPosition", "TestPerftDivide", "TestPerftEdgeCases"]
  key_links:
    - from: "tests/test_perft.py"
      to: "src/xiangqi/engine/legal.py"
      via: "import generate_legal_moves; perft calls generate_legal_moves recursively"
    - from: "tests/test_perft.py"
      to: "src/xiangqi/engine/state.py"
      via: "XiangqiState.starting() provides the test position"
---

<objective>
Implement perft (performance test) for move generation correctness. Perft recursively counts all leaf nodes at a given depth from a starting position. Results are cross-validated against CPW-verified Fairy-Stockfish reference values. Any deviation proves a move generation or legality bug.

Purpose: Gold-standard correctness gate for the entire move engine. Detects illegal moves included or legal moves missing.
Output: `tests/test_perft.py` -- perft(1)=44, perft(2)=1,920, perft(3)=79,666 verified.
</objective>

<execution_context>
@/Users/zy/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/xiangqi/engine/state.py       -- XiangqiState, copy(), king_positions
@src/xiangqi/engine/legal.py       -- generate_legal_moves, is_in_check (already implemented in 02-01)
@src/xiangqi/engine/constants.py    -- STARTING_FEN
@.planning/phases/02-move-generation/02-RESEARCH.md  -- CPW perft values: depth 1=44, 2=1,920, 3=79,666
@.planning/phases/02-move-generation/02-VALIDATION.md -- TEST-01 mapping, perft test instructions
</context>

<interfaces>
<!-- Key functions available from 02-01 implementations. -->

From src/xiangqi/engine/state.py:
```python
class XiangqiState:
    board: np.ndarray
    turn: int
    move_history: list[int]
    king_positions: Dict[int, int]

    def copy(self) -> XiangqiState: ...

    @classmethod
    def starting(cls) -> XiangqiState: ...
```

From src/xiangqi/engine/legal.py (implemented in 02-01):
```python
def generate_legal_moves(state: XiangqiState) -> list[int]: ...
def is_in_check(state: XiangqiState, color: int) -> bool: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_perft.py with CPW-verified reference values</name>
  <files>tests/test_perft.py</files>
  <read_first>src/xiangqi/engine/state.py, src/xiangqi/engine/legal.py, src/xiangqi/engine/constants.py</read_first>
  <action>
Create `tests/test_perft.py`.

**CRITICAL: Use CPW-verified values, NOT the incorrect REQUIREMENTS.md values.**

| Depth | CPW Reference (correct) | REQUIREMENTS.md (INCORRECT -- DO NOT USE) |
|-------|------------------------|------------------------------------------|
| 1     | 44                     | 44                                       |
| 2     | 1,920                  | 1,916                                    |
| 3     | 79,666                 | 72,987                                   |

The discrepancy (4 nodes at depth 2, 6,679 nodes at depth 3) is documented in 02-RESEARCH.md §State of the Art. CPW + Fairy-Stockfish CI confirmation is authoritative.

```python
"""Perft (performance test) for move generation correctness.

Perft recursively counts all leaf nodes (legal positions) at a given depth
from a starting position. Reference values are CPW-verified via Fairy-Stockfish.

CRITICAL: REQUIREMENTS.md contains incorrect perft numbers for depth 2 and 3.
Always use CPW reference values below.

Reference (CPW / Fairy-Stockfish):
  depth 1: 44   (nodes at depth 1, after 1 half-move)
  depth 2: 1,920 (nodes at depth 2, after 2 half-moves)
  depth 3: 79,666 (nodes at depth 3, after 3 half-moves)
  depth 4: 3,290,240 (CPW verified but slow for unit tests)

Source: https://www.chessprogramming.org/Chinese_Chess_Perft_Results
        Fairy-Stockfish CI perft.sh
"""

import pytest

from src.xiangqi.engine.state import XiangqiState
from src.xiangqi.engine.legal import generate_legal_moves, is_in_check


# CPW-verified reference values (CORRECT -- use these)
CPW_PERFT = {
    1: 44,
    2: 1920,
    3: 79666,
    4: 3_290_240,
}

# REQUIREMENTS.md values (INCORRECT -- for reference only, do not assert against)
REQUIREMENTS_PERFT = {
    1: 44,
    2: 1916,   # WRONG -- correct is 1,920
    3: 72987,  # WRONG -- correct is 79,666
}


def _perft(state: XiangqiState, depth: int) -> int:
    """Recursively count all leaf nodes at given depth from position.

    depth=0: return 1 (current position counts as a leaf)
    depth>0: sum perft(child) for all legal moves
    """
    if depth == 0:
        return 1
    moves = generate_legal_moves(state)
    if depth == 1:
        return len(moves)
    total = 0
    for move in moves:
        snap = state.copy()
        _apply_move(snap, move)
        total += _perft(snap, depth - 1)
    return total


def _apply_move(state: XiangqiState, move: int) -> int:
    """Minimal apply_move for perft (no Zobrist update needed for perft correctness)."""
    from src.xiangqi.engine.types import sq_to_rc
    from_sq = move & 0x1FF
    to_sq = (move >> 9) & 0x7F
    fr, fc = sq_to_rc(from_sq)
    tr, tc = sq_to_rc(to_sq)
    piece = int(state.board[fr, fc])
    captured = int(state.board[tr, tc])
    state.board[tr, tc] = state.board[fr, fc]
    state.board[fr, fc] = 0
    if abs(piece) == 1:
        state.king_positions[state.turn] = to_sq
    state.turn *= -1
    return captured


class TestPerftStartingPosition:
    """Verify move generation against CPW/Fairy-Stockfish reference values."""

    def test_perft_depth_1(self):
        """Depth 1: all legal moves from starting position = 44."""
        state = XiangqiState.starting()
        count = _perft(state, depth=1)
        assert count == CPW_PERFT[1], f"perft(1): expected {CPW_PERFT[1]}, got {count}"

    def test_perft_depth_2(self):
        """Depth 2: all nodes at depth 2 = 1,920 (CPW verified; NOT 1,916 from REQUIREMENTS.md)."""
        state = XiangqiState.starting()
        count = _perft(state, depth=2)
        assert count == CPW_PERFT[2], f"perft(2): expected {CPW_PERFT[2]}, got {count}"

    def test_perft_depth_3(self):
        """Depth 3: all nodes at depth 3 = 79,666 (CPW verified; NOT 72,987 from REQUIREMENTS.md).

        This is the critical gate: most move generation bugs appear by depth 3.
        The ~6,679 node gap vs REQUIREMENTS.md (72,987) proves REQUIREMENTS.md is wrong.
        """
        state = XiangqiState.starting()
        count = _perft(state, depth=3)
        assert count == CPW_PERFT[3], f"perft(3): expected {CPW_PERFT[3]}, got {count}"

    def test_perft_depth_4_reference(self):
        """Depth 4: 3,290,240 (CPW verified). Run only as a reference; too slow for CI."""
        state = XiangqiState.starting()
        count = _perft(state, depth=4)
        assert count == CPW_PERFT[4], f"perft(4): expected {CPW_PERFT[4]}, got {count}"

    def test_requirements_md_values_fail(self):
        """Prove REQUIREMENTS.md values are wrong by showing they do NOT match CPW.

        This test documents the discrepancy. If REQUIREMENTS.md is ever corrected,
        this test should be removed or updated.
        """
        state = XiangqiState.starting()
        count2 = _perft(state, depth=2)
        count3 = _perft(state, depth=3)
        # REQUIREMENTS.md says 1,916 but CPW is 1,920
        assert count2 != REQUIREMENTS_PERFT[2], "Should not match incorrect REQUIREMENTS value"
        # REQUIREMENTS.md says 72,987 but CPW is 79,666
        assert count3 != REQUIREMENTS_PERFT[3], "Should not match incorrect REQUIREMENTS value"
        # Must match CPW
        assert count2 == CPW_PERFT[2]
        assert count3 == CPW_PERFT[3]


class TestPerftDivide:
    """perft_divide: break down depth-1 moves to identify which move leads to wrong subtree counts.

    This is the standard Xiangqi debugging technique. If perft(n) fails but perft(n-1) passes,
    divide isolates the buggy move.
    """

    def test_perft_divide_depth_1(self):
        """Return per-move subtree counts at depth 2 as a diagnostic.

        The sum of all depth-2 per-move subtrees must equal perft(2) = 1,920.
        Any move whose subtree sum != expected reveals a bug.
        """
        state = XiangqiState.starting()
        moves = generate_legal_moves(state)
        total = 0
        move_counts = {}
        for move in moves:
            snap = state.copy()
            _apply_move(snap, move)
            sub_count = _perft(snap, depth=1)  # depth 1 from child = depth 2 from root
            move_counts[move] = sub_count
            total += sub_count
        assert total == CPW_PERFT[2], f"Divide sum {total} != {CPW_PERFT[2]}"
        # All individual move counts should be > 0
        for move, cnt in move_counts.items():
            assert cnt > 0, f"Move {move} has 0 nodes -- indicates illegal move included"


class TestPerftEdgeCases:
    """Perft on non-standard positions to catch piece-specific bugs."""

    def test_perft_trivial_position(self):
        """Single chariot vs bare generals: chariot has 17 moves, each leads to 0 further moves."""
        from src.xiangqi.engine.types import Piece, rc_to_sq
        import numpy as np
        board = np.zeros((10, 9), dtype=np.int8)
        board[7, 4] = Piece.R_SHUAI
        board[0, 4] = Piece.B_JIANG
        board[5, 4] = Piece.R_CHE  # chariot at (5,4)
        state = XiangqiState(
            board=board, turn=+1,
            move_history=[], halfmove_clock=0,
            zobrist_hash_history=[0],
            king_positions={+1: rc_to_sq(7, 4), -1: rc_to_sq(0, 4)},
        )
        # Red has only chariot moves; general is surrounded by chariot and own general
        moves = generate_legal_moves(state)
        assert len(moves) == 17  # chariot unblocked: 17 moves
        total = _perft(state, depth=1)
        assert total == 17

    def test_no_illegal_moves_included(self):
        """Verify perft counts at each depth match CPW reference.

        If the move generator includes illegal moves, subtree sizes become inconsistent.
        """
        state = XiangqiState.starting()
        d1 = _perft(state, depth=1)
        d2 = _perft(state, depth=2)
        # Must match CPW reference
        assert d2 == CPW_PERFT[2]
        assert d1 == CPW_PERFT[1]
```
  </action>
  <verify>
python3.11 -m pytest tests/test_perft.py::TestPerftStartingPosition::test_perft_depth_1 -x -v
# Must pass: 44 nodes
python3.11 -m pytest tests/test_perft.py::TestPerftStartingPosition::test_perft_depth_2 -x -v
# Must pass: 1,920 nodes (CPW value, NOT 1,916)
python3.11 -m pytest tests/test_perft.py::TestPerftStartingPosition::test_perft_depth_3 -x -v
# Must pass: 79,666 nodes (CPW value, NOT 72,987)
python3.11 -m pytest tests/test_perft.py -v --tb=short
# All perft tests pass
  </verify>
  <done>tests/test_perft.py created with CPW-verified reference values. perft(1)=44, perft(2)=1,920, perft(3)=79,666 all pass. Depth-4 reference test included.</done>
</task>

</tasks>

<verification>
After execution:
```
python3.11 -m pytest tests/test_perft.py -v --tb=short
```
All tests pass. Key assertions:
- perft(1) = 44
- perft(2) = 1,920 (CPW verified, NOT 1,916 from REQUIREMENTS.md)
- perft(3) = 79,666 (CPW verified, NOT 72,987 from REQUIREMENTS.md)
- test_requirements_md_values_fail proves REQUIREMENTS.md values are wrong
- perft(4) = 3,290,240 verified (reference test, may be slow)
</verification>

<success_criteria>
- tests/test_perft.py exists with all perft tests
- `python3.11 -m pytest tests/test_perft.py -v` -- all green
- perft(1) = 44, perft(2) = 1,920, perft(3) = 79,666 (CPW reference)
- test_requirements_md_values_fail proves REQUIREMENTS.md values are wrong
- perft(4) = 3,290,240 verified (reference test, may be slow)
</success_criteria>

<output>
After completion, create `.planning/phases/02-move-generation/02-02-SUMMARY.md`
</output>
