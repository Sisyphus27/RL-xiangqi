---
phase: 05-board-rendering
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/xiangqi/ui/board.py
autonomous: true
gap_closure: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Application launches without TypeError from drawLine"
    - "Grid lines (vertical, horizontal, palace diagonals) render correctly"
    - "All 28 automated UI tests pass"
  artifacts:
    - path: "src/xiangqi/ui/board.py"
      provides: "QXiangqiBoard with fixed drawLine calls"
      contains: "QLineF"
      min_lines: 194
  key_links:
    - from: "src/xiangqi/ui/board.py"
      to: "PyQt6.QtCore.QLineF"
      via: "QLineF wrapper for float-coordinate drawLine calls"
      pattern: "p.drawLine\\(QLineF\\("
---

<objective>
Fix the remaining PyQt6 TypeError in board.py drawBackground(). After plan 05-05 fixed fillRect() by wrapping float coords in QRectF, a second identical pattern surfaces in drawLine(). All drawLine() calls with float coordinates must be wrapped in QLineF.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/xiangqi/ui/board.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap all drawLine float-coordinate calls in QLineF</name>
  <read_first>
    - src/xiangqi/ui/board.py
  </read_first>
  <action>
Apply two changes to src/xiangqi/ui/board.py:

**Change 1 — Add QLineF to the import (line 16):**
```
from PyQt6.QtCore import Qt, QRectF, QPointF, QLineF
```

**Change 2 — Wrap every drawLine call in QLineF:**

Category A — Vertical grid lines (line 143):
- BEFORE: `p.drawLine(x, 0.6 * cell, x, 10.6 * cell)`
- AFTER:  `p.drawLine(QLineF(x, 0.6 * cell, x, 10.6 * cell))`

Category B — Horizontal grid lines (line 150):
- BEFORE: `p.drawLine(0.6 * cell, y, 9.6 * cell, y)`
- AFTER:  `p.drawLine(QLineF(0.6 * cell, y, 9.6 * cell, y))`

Category C — Palace diagonal lines (lines 154-162, 8 calls total):
- BEFORE: `p.drawLine((0.6) * cell, (0.6) * cell, (2.6) * cell, (2.6) * cell)`
- AFTER:  `p.drawLine(QLineF((0.6) * cell, (0.6) * cell, (2.6) * cell, (2.6) * cell))`

Apply the same QLineF wrapper pattern to all remaining drawLine calls on lines 155-162:
- Line 155: `p.drawLine(QLineF((2.6) * cell, (0.6) * cell, (0.6) * cell, (2.6) * cell))`
- Line 156: `p.drawLine(QLineF((6.6) * cell, (0.6) * cell, (8.6) * cell, (2.6) * cell))`
- Line 157: `p.drawLine(QLineF((8.6) * cell, (0.6) * cell, (6.6) * cell, (2.6) * cell))`
- Line 159: `p.drawLine(QLineF((0.6) * cell, (7.6) * cell, (2.6) * cell, (9.6) * cell))`
- Line 160: `p.drawLine(QLineF((2.6) * cell, (7.6) * cell, (0.6) * cell, (9.6) * cell))`
- Line 161: `p.drawLine(QLineF((6.6) * cell, (7.6) * cell, (8.6) * cell, (9.6) * cell))`
- Line 162: `p.drawLine(QLineF((8.6) * cell, (7.6) * cell, (6.6) * cell, (9.6) * cell))`

No other changes to the file are needed.
  </action>
  <verify>
    <automated>grep -n "QLineF" src/xiangqi/ui/board.py | wc -l && grep -c "p.drawLine(QLineF(" src/xiangqi/ui/board.py</automated>
  </verify>
  <done>
    All 10 drawLine calls in drawBackground() use QLineF wrapper.
    QLineF is imported from PyQt6.QtCore at line 16.
    Application launches without TypeError.
    All 28 automated UI tests pass.
  </done>
</task>

</tasks>

<verification>
After execution, run:
1. `pytest tests/ui/ -v` — all 28 tests pass
2. `python -m src.xiangqi.ui.main` — launches without TypeError (verify manually)
3. `grep -c "p.drawLine(QLineF(" src/xiangqi/ui/board.py` — returns 10
</verification>

<success_criteria>
- board.py line 16 imports QLineF
- All 10 drawLine calls in drawBackground() wrapped in QLineF
- grep "p.drawLine(QLineF(" returns 10 matches
- pytest tests/ui/ -v exits with 0 (all 28 tests pass)
- Application launches without TypeError at board.py line 143
</success_criteria>

<output>
After completion, create `.planning/phases/05-board-rendering/05-06-SUMMARY.md`
</output>
