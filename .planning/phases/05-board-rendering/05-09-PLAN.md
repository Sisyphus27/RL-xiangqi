---
phase: 05-board-rendering
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/xiangqi/ui/board.py
autonomous: true
gap_closure: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Vertical grid lines stay within board boundaries (y = 0.6*cell to y = 9.6*cell)"
    - "Vertical lines do not extend beyond the last horizontal line at row 9"
  artifacts:
    - path: "src/xiangqi/ui/board.py"
      provides: "QXiangqiBoard with bounded vertical grid lines"
      contains: "9.6 * cell"
      min_lines: 194
  key_links:
    - from: "src/xiangqi/ui/board.py line 143"
      to: "Vertical grid line rendering"
      via: "drawLine(QLineF(x, y1, x, y2))"
      pattern: "10\\.6.*cell"
---

<objective>
Fix vertical line boundary overflow bug. Vertical lines currently extend from y = 0.6*cell to y = 10.6*cell, but the grid only has 10 horizontal lines (rows 0-9, ending at y = 9.6*cell). The vertical lines extend one cell beyond the bottom boundary, making them appear to "connect to the very edge" at the red side (bottom).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/xiangqi/ui/board.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vertical line end coordinate to match grid boundary</name>
  <read_first>
    - src/xiangqi/ui/board.py
  </read_first>
  <action>
Fix the vertical grid line end coordinate in src/xiangqi/ui/board.py at line 143.

**Root Cause Analysis:**
The vertical lines currently use y-end coordinate of 10.6*cell, but the grid only spans from row 0 (y = 0.6*cell) to row 9 (y = 9.6*cell). The horizontal lines are drawn at:
- Row 0: y = 0.6*cell
- Row 1: y = 1.6*cell
- ...
- Row 9: y = 9.6*cell (LAST horizontal line)

The vertical lines extending to y = 10.6*cell go ONE CELL beyond the last horizontal line, causing them to visually extend past the board boundary at the red side (bottom).

**Grid coordinate math:**
- Grid top: y = 0.6*cell
- Grid bottom (row 9): y = 9.6*cell
- Vertical lines should span: y = 0.6*cell to y = 9.6*cell (NOT 10.6*cell)

**Current incorrect code (line 143):**
```python
            p.drawLine(QLineF(x, 0.6 * cell, x, 10.6 * cell))
```

**Corrected code:**
```python
            p.drawLine(QLineF(x, 0.6 * cell, x, 9.6 * cell))
```

Change `10.6 * cell` to `9.6 * cell` so vertical lines end at the same y-coordinate as the last horizontal line (row 9).

**Why this fixes the bug:**
- Before: Vertical lines extend to y = 10.6*cell (beyond the grid)
- After: Vertical lines end at y = 9.6*cell (at the bottom boundary line)
- Red side's vertical lines no longer appear to extend beyond the board edge
</action>
  <verify>
    <automated>grep "10.6.*cell" src/xiangqi/ui/board.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - board.py vertical line loop uses `9.6 * cell` as y-end coordinate
    - grep "10.6.*cell" src/xiangqi/ui/board.py returns 0 (no 10.6*cell references remain)
    - pytest tests/ui/ -v exits with 0
  </acceptance_criteria>
  <done>
    Vertical grid lines end at y = 9.6*cell (matching row 9 horizontal line).
    Vertical lines do not extend beyond board boundary.
    All 28 automated UI tests pass.
  </done>
</task>

</tasks>

<verification>
After execution, run:
1. `grep "10.6.*cell" src/xiangqi/ui/board.py` - returns 0 (no matches)
2. `grep "9.6.*cell" src/xiangqi/ui/board.py | grep drawLine` - shows vertical line with correct end coordinate
3. `pytest tests/ui/ -v` - all 28 tests pass
4. Visual verification: Red side's vertical lines end at the bottom boundary, do not extend beyond
</verification>

<success_criteria>
- board.py vertical grid line uses `9.6 * cell` as y-end coordinate (not 10.6*cell)
- No `10.6` appears anywhere in board.py
- pytest tests/ui/ -v exits with 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-board-rendering/05-09-SUMMARY.md`
</output>
